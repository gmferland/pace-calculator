<!DOCTYPE html>
<html lang="en">

<head>
  <title>Running Pace Calculator | Race Pace Space</title>
  <meta name="title" content="Running Pace Calculator" />
  <meta name="description"
    content="The running pace calculator. All grit, no shit. Calculate your training paces, race paces, and whatever other things you're trying to pace." />
  <meta name="keywords"
    content="race pace calculator, pace calculator, running calculator, training calculator, athletics calculator" />
  <meta name="robots" content="index, follow" />
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="language" content="English" />
  <meta name="revisit-after" content="3 days" />
  <meta name="viewport" content="initial-scale=1.0, width=device-width">

  <script async src="https://www.googletagmanager.com/gtag/js?id=UA-116117867-2"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag() {
      dataLayer.push(arguments);
    }
    gtag('js', new Date());
    gtag('config', 'UA-116117867-2');
  </script>
</head>

<body>
  <header class="header">
    <nav class="nav">
      <div class="container">
        <h1>Pace Calculator</h1>
      </div>
    </nav>

    <div class="container">
      <form id="input-form">
        <label class="flex-column input-container" for="race">
          <span>Race Distance</span>
          <input type="text" id="race" name="race" list="race-options" placeholder="Enter Distance" />
          <datalist id="race-options">
            <% htmlWebpackPlugin.options.raceOptions.forEach(function(option) {
                  %>
            <option value="<%= option.name %>"><%- option.name %></option>
            <% }) %>
          </datalist>
        </label>
        <label class="flex-column input-container" for="time">
          Goal Time
          <input id="time" type="text" name="time" placeholder="Format hh:mm:ss, mm:ss, or ss" />
        </label>
        <div class="flex-row input-unit">
          <% htmlWebpackPlugin.options.units.forEach(function(unit) { %>
          <div class="input-container">
            <input type="radio" name="unit" id="<%= unit.name %>" value="<%= unit.id %>" />
            <label for="<%= unit.name %>"><%- unit.name %></label>
          </div>
          <% }) %>
        </div>
        <button type="submit" class="submit-button">Calculate</button>
      </form>
    </div>
  </header>

  <div class="flex-column main container">
    <span id="error-label" class="hidden"></span>
    <div id="results" class="flex-column">
      <h2 id="splits-table-title" class="hidden">Splits</h2>
      <div id="splits-table" class="flex-column"></div>
    </div>
  </div>
</body>

</html>
